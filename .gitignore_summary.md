# 忽略配置快速参考

## ✅ 已配置完成

- ✅ `.gitignore` - Git 版本控制忽略
- ✅ `.cursorignore` - Cursor AI 索引忽略
- ✅ `data/` - 数据目录（保留结构）
- ✅ `data/audio/` - 音频目录（保留结构）
- ✅ `uploads/` - 上传目录（保留结构）

## 📝 主要忽略内容

### Git (.gitignore)
```
✗ data/                    # 所有数据文件
✗ *.db, *.sqlite          # 数据库
✗ logs/*.log              # 日志
✗ uploads/                # 上传文件
✗ __pycache__/            # Python 缓存
✗ .venv/                  # 虚拟环境
✗ models/                 # ASR 模型
✗ *.wav, *.mp4           # 大型媒体文件
```

### Cursor (.cursorignore)
```
✗ 所有 .gitignore 的内容
✗ uv.lock                 # 大型依赖锁
✗ *.safetensors          # 大型模型文件
✗ docs/_build/           # 文档构建输出
```

## 🧪 测试结果

```bash
# 已验证以下文件被正确忽略：
✓ data/test.db           → 被 .gitignore:47 匹配
✓ data/audio/test.wav    → 被 .gitignore:47 匹配
✓ uploads/test.jpg       → 被 .gitignore:59 匹配
```

## 💡 常用命令

```bash
# 查看被忽略的文件
git status --ignored

# 测试文件是否被忽略
git check-ignore -v <file>

# 强制添加被忽略的文件
git add -f <file>

# 清理被忽略的文件（小心）
git clean -fdX
```

## 📂 目录结构

```
aira/
├── .gitignore          ← Git 忽略配置
├── .cursorignore       ← Cursor 忽略配置
├── data/               ← 数据目录（被忽略）
│   ├── .gitkeep       ← 保持目录
│   ├── conversations.db
│   └── audio/
│       ├── .gitkeep
│       └── *.wav
├── uploads/            ← 上传目录（被忽略）
│   └── .gitkeep
└── logs/              ← 日志目录（被忽略）
    └── *.log
```

## 📖 详细文档

完整说明请查看：`GITIGNORE_INFO.md`

